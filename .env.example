NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Site key used by CampaignPlayer's own preview (dogfooding the placements
# API from inside the dashboard, not a real publisher embed). Register one
# via the /sites page (as a PUBLISHER or ADMIN user) with no origin/bundle ID
# restriction, then paste its siteKey here. Blank until you do this --
# CampaignPlayer shows an explanatory message instead of a live ad preview.
NEXT_PUBLIC_PREVIEW_SITE_KEY=
